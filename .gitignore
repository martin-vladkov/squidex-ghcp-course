# User-specific files
*.log
*.bak
*.suo
*.user
*.vs
*.received.txt

.angular
.awCache
.git
.github
.idea
.vscode

# Build results
bin/
build/
obj/
object/
out/
publish/

# Test Output
_test-output/

# NodeJS 
node_modules/

/backend/src/Squidex/Assets

appsettings.Development.json
appsettings.Production.json
launchSettings.json

/frontend/app-config/localhost-key.pem
/frontend/app-config/localhost.pem

# Private keys & certificates (never commit private key material)
# The dev/*.pfx is intentionally tracked (needed by install-cert.ps1) but is
# protected only by a known dev password — treat as low-privilege localhost only.
dev/*.key
dev/*.p12
*.key
*.p12

# Environment / secret files
.env
.env.*
*.env
!*.env.example
secrets.json
**/secrets.json
**/*secrets*.json
!**/*.example.json

# C# user secrets override (appsettings already covered above)
appsettings.*.json
!appsettings.json

# C# language server cache
*.lscache
